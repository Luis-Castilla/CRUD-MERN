import React from 'react';
import NavBar from '../../parts/Navbar/Navbar';
import ListClients from '../../parts/ListClients/ListClients';

const Home = () => {
    return (
        <div>
            <NavBar />
            <ListClients />
        </div>
    );
}

export default Home;